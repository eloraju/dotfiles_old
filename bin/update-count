#!/bin/sh
sudo pacman -Syyup 1>/dev/null 2>/dev/null
OFFICIAL="$(yay -Qu --repo | wc -l)"
AUR="$(yay -Qua | wc -l)"
COUNTS="{$OFFICIAL} ~~ [$AUR]"
echo "<action=xdotool key super+alt+shift+control+u>$COUNTS</action>"
#echo "$OFFICIAL <fn=1></fn> $AUR"
